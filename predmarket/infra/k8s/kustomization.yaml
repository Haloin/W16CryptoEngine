apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: predmarket

resources:
  - namespace.yaml
  - secret.yaml
  - configmap.yaml
  - postgres.yaml
  - nats.yaml
  - engine.yaml
  - api.yaml

commonLabels:
  app.kubernetes.io/name: predmarket
  app.kubernetes.io/managed-by: kustomize
